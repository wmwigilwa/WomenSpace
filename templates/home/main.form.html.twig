{% extends 'landing.base.html.twig' %}
{% block main_body %}
<body>

    <div class="grid lg:grid-cols-3">
        <div class="h-screen hidden lg:block lg:col-span-2 h-10 login-cover bg-blue-200">
                <div class="container items-center justify-center">
                </div>
        </div>
        <div class="h-screen">
            <div class="flex flex-col items-center justify-center px-6 py-8 mx-auto md:h-screen lg:py-0">
                <div class="w-full md:mt-0 sm:max-w-md xl:p-0">
                    <p class="p-6 sm:p-8">
                        <a href="{{ path('app_home') }}" class="font-medium text-gray-500 hover:text-gray-600"><i class="fa-solid fa-arrow-left-long mr-3"></i> Back to Home</a>
                    </p>
                </div>
                <a href="{{ path('app_login') }}">
                    <img src="{{ asset('build/images/partial_logo.png') }}" alt="logo" class="login-logo"/>
                </a>
                {% include form_template %}
            </div>
        </div>
    </div>

</body>

{% endblock %}

